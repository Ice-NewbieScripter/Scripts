task.spawn(function()
    -- Wait for the game to fully load
    if not game:IsLoaded() then
        game.Loaded:Wait()
    end

    -- Get player and character
    local player = game.Players.LocalPlayer
    if not player.Character then
        player.CharacterAdded:Wait()
    end
    local character = player.Character or player.CharacterAdded:Wait()
    local hrp = character:WaitForChild("HumanoidRootPart")

    -- Freeze and teleport to SE3 safe zone (far south island)
    hrp.Anchored = true
    task.wait(0.1)
    hrp.CFrame = CFrame.new(-201.54, 3.14, -29734.01)

    local maxRange = 300  -- Max detection range for the gun
    local done = false

    -- Attempt to sit on any available MaximGun
    local function trySit(gun)
        if gun then
            local seat = gun:FindFirstChild("VehicleSeat") or gun:FindFirstChildWhichIsA("VehicleSeat", true)
            if seat and seat:IsA("VehicleSeat") and (seat.Position - hrp.Position).Magnitude <= maxRange then
                seat.Disabled = false
                hrp.CFrame = seat.CFrame * CFrame.new(0, 1.5, 0)  -- Slight upward offset = 100% sit success
                done = true
            end
        end
    end

    -- Continuously scan for a MaximGun until seated
    while not done do
        task.defer(function()
            local safeZoneContainer = workspace:FindFirstChild("SafeZones")
            local safeZone = safeZoneContainer and safeZoneContainer:FindFirstChild("SafeZone")
            if not safeZone then return end

            -- Type 1: Placed as sentry turret
            local gun1 = safeZone:FindFirstChild("Sentries")
            gun1 = gun1 and gun1:FindFirstChild("TurretSpot")
            gun1 = gun1 and gun1:FindFirstChild("MaximGun")
            trySit(gun1)

            -- Type 2: On the gunsmith shop table
            local gun2 = safeZone:FindFirstChild("Buildings")
            gun2 = gun2 and gun2:FindFirstChild("Gunsmith")
            gun2 = gun2 and gun2:FindFirstChild("GunTable")
            gun2 = gun2 and gun2:FindFirstChild("ShopItems")
            gun2 = gun2 and gun2:FindFirstChild("MaximGun")
            trySit(gun2)
        end)

        task.wait()  -- Minimal delay to avoid overloading
    end

    task.wait(0.2)
    hrp.Anchored = false  -- Unfreeze once you're locked in
end)
